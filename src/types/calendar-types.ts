export interface CalendarData {
  scheduleId: number,
  title: string
  scheduleType: string
  startDate: string;
  endDate: string;
}

export interface YearMonth{
     year: number,
    month: number
}